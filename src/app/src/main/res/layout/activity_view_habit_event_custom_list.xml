<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/content_view"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/habit_details_background"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="@color/white"
        app:layout_constraintDimensionRatio="7.064:1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:shapeAppearanceOverlay="@style/roundedCorners" />

    <TextView
        android:id="@+id/date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="28dp"
        android:text="Date"
        android:textColor="#3C85B9"
        android:textSize="14sp"
        app:layout_constraintBottom_toBottomOf="@+id/habit_details_background"
        app:layout_constraintStart_toStartOf="@+id/habit_details_background"
        app:layout_constraintTop_toTopOf="@+id/habit_details_background" />

    <ImageView
        android:id="@+id/image_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_image_negative"
        android:layout_marginEnd="28dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintRight_toRightOf="@id/habit_details_background"
        app:layout_constraintTop_toTopOf="parent" />

    <ImageView
        android:id="@+id/comment_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        android:src="@drawable/ic_comment_negative"
        app:layout_constraintEnd_toStartOf="@id/image_icon"
        app:layout_constraintTop_toTopOf="@+id/image_icon" />

    <ImageView
        android:id="@+id/location_icon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        android:src="@drawable/ic_map_marker_negative"
        app:layout_constraintEnd_toStartOf="@id/comment_icon"
        app:layout_constraintTop_toTopOf="@+id/image_icon" />

</androidx.constraintlayout.widget.ConstraintLayout>

